import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'littlejsengine-ts-template',
  webDir: 'dist'
};

export default config;
